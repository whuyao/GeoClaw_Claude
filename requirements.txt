# GeoClaw v0.1 — Python 依赖清单
# 安装命令: pip install -r requirements.txt
#
# 如使用 conda:
#   conda install -c conda-forge geopandas shapely pyproj rasterio
#   pip install folium osmnx matplotlib-scalebar mapclassify contextily

# ── 核心 GIS 依赖 ─────────────────────────────────────────────────────────────
geopandas>=1.0.0        # 空间数据处理核心
shapely>=2.0.0          # 几何计算
pyproj>=3.5.0           # 坐标参考系统
rasterio>=1.3.0         # 栅格数据（v0.2 栅格模块需要）
pyogrio>=0.5.0          # 高性能矢量 I/O 后端（geopandas 依赖）

# ── 数据处理 ──────────────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0           # KDE 核密度估计

# ── 网络分析 ──────────────────────────────────────────────────────────────────
networkx>=3.0           # 图论网络分析

# ── 制图可视化 ────────────────────────────────────────────────────────────────
matplotlib>=3.7.0
matplotlib-scalebar>=0.8.0  # 地图比例尺
folium>=0.14.0          # 交互式 Web 地图
mapclassify>=2.5.0      # Choropleth 分级方案（quantiles/jenks/等）
contextily>=1.3.0       # 底图切片（OSM/Stamen/ESRI，v0.2 使用）

# ── OSM 数据获取 ──────────────────────────────────────────────────────────────
requests>=2.28.0        # HTTP 请求（Overpass API）
osmnx>=1.6.0            # 城市边界/路网下载

# ── 开发工具（可选）──────────────────────────────────────────────────────────
# pytest>=7.0          # 单元测试运行器 (pip install pytest)
# jupyter>=1.0         # Notebook 环境 (pip install jupyter)
# black>=23.0          # 代码格式化 (pip install black)
